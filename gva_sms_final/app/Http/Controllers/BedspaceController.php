<?php

namespace App\Http\Controllers;

use App\Models\Bedspace;
use Illuminate\Http\Request;

class BedspaceController extends Controller
{
    // public function getBedspaces($hostel_id)
    // {
    //     // Fetch bedspaces for the selected hostel
    //     $bedspaces = Bedspace::where('hostel_id', $hostel_id)->get();

    //     // Return the bedspaces in JSON format for the frontend to process
    //     return response()->json($bedspaces);
    // }
    
}
